services:
  kiro-claude-proxy:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - DEBUG=${DEBUG:-false}
    volumes:
      - "~/Library/Application Support/kiro-cli:/root/.config/kiro-cli:ro"
    restart: unless-stopped
